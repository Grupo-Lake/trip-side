// Placeholder credentials — hardcoded on purpose until real fan/customer accounts exist.
// TODO: replace with real authentication (customer sign-up/login) before going to production.
// Split from lib/auth.ts (server-only) so the login form can display the demo hint client-side.
export const PLACEHOLDER_USUARIO = "fatripside";
export const PLACEHOLDER_SENHA = "tripside2026";
